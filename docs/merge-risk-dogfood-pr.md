# Merge risk dogfood pull request

This file exists only to open a pull request that runs the repository merge-risk workflow against **base-branch** `.merge-risk.yml` (including **`branch_age`**).

Safe to delete after the dogfood run.
